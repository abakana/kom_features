/* groovylint-disable-next-line CompileStatic */
pipeline {
     agent any

    stages {
        stage('Start') {
            steps {
                echo 'nu start'
            }
        }
        stage('test') {
            steps {
                echo 'nu test'
            }
        }
    }
}
