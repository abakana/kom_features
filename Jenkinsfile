/* groovylint-disable-next-line CompileStatic */
pipeline {
    agent {
        label any
    }

    stages {
        stage('test') {
            steps {
                echo 'nu privet'
            }
        }
    }
}
